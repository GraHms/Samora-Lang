let adder = fn(x) {
  fn(y) {
    x + y;
  };
};
// adder is a function that returns a function
// addTwo is a function that takes a number and returns a number
let addTwo = adder(2.3);
let result = addTwo(3);
print("2.3 + 3 = ", result);
